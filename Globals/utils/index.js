const objectIsEmpty = require('./ObjectIsEmpty.js')
const errorLog = require('./errorLog')
const validate = require('./validate')
const handleReturnDB = require('./handleReturnDB')

module.exports = {
    objectIsEmpty,
    errorLog,
    validate,
    handleReturnDB,
}